# Web layer (public page + API):
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
httpx>=0.27.0            # HTTP client for the Amazon 查询系统 tool (tools/amazon.py)
python-multipart>=0.0.9  # file upload for POST /api/analyze

# Model providers — install the one(s) you use:
openai>=1.30.0           # OpenAI-compatible provider (Alibaba Bailian / Qwen, etc.)
anthropic>=0.39.0        # real Claude provider (fill in ANTHROPIC_API_KEY)
python-dotenv>=1.0.0     # load .env into the environment (optional)

# Dev / test:
pytest>=7.0.0

# File analysis (docparse/docanalysis) — OPTIONAL. Only needed to parse those
# formats; import is lazy so the harness runs without them. csv/txt need nothing.
openpyxl>=3.1              # .xlsx (also used by data_loader)
pdfplumber>=0.11           # .pdf (text-based; scanned PDFs need OCR, not supported)
python-docx>=1.1           # .docx
python-pptx>=0.6           # .pptx (optional)

# Note: the core harness itself (tradeflow/) has ZERO required deps and runs on
# MockProvider. The above are for serving it as a web app / going live.
